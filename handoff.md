# 交接记录

## 当前目标

- 维护轻量、标准库限定、可跨项目复用的 AI 工程系统，并管理同一家公司的共享岗位和业务部门。

## 已完成

- 通用核心、按需知识、工程专员、任务流程、安装模板、工具适配器、公司共享岗位和 H5 游戏部门已建立。
- CLI 已覆盖创建、安装、预览、更新、验证、移除和导出，自动化测试覆盖主要生命周期。
- 2026-08-24 完成本地私有仓库准备：增加保守忽略/属性规则、仓库泄露守卫及测试、双语操作与安全文档、跨平台迁移/回滚指南、开源评审记录和固定版本的 CI 配置。
- 文档目录、公司入口、健康检查和导出范围已与仓库准备结果对齐。

## 关键决定

- 中央 `ai-system` 与业务项目保持同级，源码拉取不会自动修改任何业务项目。
- 对目标执行 `init` 或 `update` 前必须先使用 `--dry-run`；目标应先建立干净恢复点。
- 项目继续只依赖 Git 与 Python 3.10+；外部 Gitleaks 是发布前扫描工具，不作为仓库运行时依赖或内置副本。
- 公司岗位与业务部门留在 `company/`；供应商中立规则留在 `core/`，工具语法留在 `adapters/`。

## 风险与待确认

- 所有权和公开许可尚未决定，因此没有添加许可证，也不应假定可公开分发。
- CI 配置尚未在远端仓库运行；Python 3.10 与 3.13 的本地/CI 差异仍需首次远端执行验证。
- 尚未用第二个真实项目完整验证安装、更新和回滚体验。
- GitHub 浏览器会话当前停留在登录页；完成账号登录和设备授权前，无法创建远端仓库或推送。

## 剩余远端发布步骤

- 由有权限的操作人员复核本地差异和秘密扫描结果，再初始化 Git、创建私有远端、提交并推送；这些动作本次均未执行。
- 登录 GitHub 后重新完成 CLI 设备授权，查询账号并确认 `ai-system` 远端名称可用。
- 首次推送前对工作树和历史运行 Gitleaks，并按实际托管策略确认仓库可见性、成员权限、默认分支和分支保护。
- 首次远端 CI 通过后记录结果；若未来改变可见性或发布方式，先重新核验许可证、秘密、历史和外部 Action 版本。
